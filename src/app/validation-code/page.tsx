import ContainerValidationCode from '@/components/containers/ContainerValidationCode';
import { metadata } from '../layout';

export default function ValidationCodePage() {
  metadata.title = 'Routinely - Redefinir senha';
  return <ContainerValidationCode />;
}
